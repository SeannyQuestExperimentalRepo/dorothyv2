# ─── Database (Neon PostgreSQL) ──────────────────────────────────
DATABASE_URL="postgresql://user:password@host:5432/trendline?schema=public&connection_limit=5&pool_timeout=10"

# ─── Cron Secret (Vercel Cron job authentication) ────────────────
# Generate with: openssl rand -base64 32
CRON_SECRET=""

# ─── NextAuth.js v5 ─────────────────────────────────────────────
# Generate with: openssl rand -base64 32
AUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# ─── Google OAuth ────────────────────────────────────────────────
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ─── OpenAI (LLM search query interpretation) ───────────────────
OPENAI_API_KEY=""

# ─── The Odds API (live odds for upcoming games) ────────────────
THE_ODDS_API_KEY=""

# ─── OpenWeatherMap (weather forecasts for upcoming games) ───────
OPENWEATHER_API_KEY=""

# ─── KenPom API (college basketball ratings & efficiency data) ────
KENPOM_API_KEY=""

# ─── CBBData (Barttorvik T-Rank archive) ─────────────────────────
CBD_API_KEY=""

# ─── Sentry (error monitoring & performance) ─────────────────────
NEXT_PUBLIC_SENTRY_DSN=""
SENTRY_AUTH_TOKEN=""
SENTRY_ORG=""
SENTRY_PROJECT=""

# ─── Web Push (VAPID keys for push notifications) ─────────────
# Generate with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_EMAIL="mailto:admin@trendline.app"

# ─── Stripe (subscription billing — inactive until launch) ──────
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
STRIPE_PRICE_MONTHLY=""
STRIPE_PRICE_ANNUAL=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""
